import 'package:flutter/material.dart';

class ShopDetailReviewsTabWidget extends StatelessWidget {
  const ShopDetailReviewsTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Padding(
        padding: EdgeInsets.all(32),
        child: Text('Teswirler heniz ýok'),
      ),
    );
  }
}