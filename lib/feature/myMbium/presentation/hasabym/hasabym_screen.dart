import 'package:flutter/material.dart';

class HasabymScreen extends StatefulWidget {
  const HasabymScreen({super.key});

  @override
  State<HasabymScreen> createState() => _HasabymScreenState();
}

class _HasabymScreenState extends State<HasabymScreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('hasabym')));
  }
}
